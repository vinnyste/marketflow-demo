// A tela de detalhes é compartilhada com o painel administrativo,
// mas é renderizada dentro do layout operacional. Assim o operador
// não é redirecionado para uma rota protegida exclusiva de administrador.
export { default } from '@/app/admin/orders/[id]';
